# Enable strict peer dependencies
strict-peer-dependencies=false

# Use the npm registry
registry=https://registry.npmjs.org/

# Don't create package-lock.json (we use pnpm)
package-lock=false

# Provenance for npm publish
provenance=true
